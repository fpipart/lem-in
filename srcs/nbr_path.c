
static int find_pathlength(t_lem *lem, char *end, int path)
{
	int len;

	len = 0;
}

int		choose_paths(t_lem *lem, t_store *store, int path_nbr)
{
	int i;
	int size_path;

	size_path = find_pathlength(lem, store->end, 1);
	i = store->ants + size_path;

	return (0);
}
